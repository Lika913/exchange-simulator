import { Side } from "../types/order/side";

export const COLORS_SIDE: Record<Side, string> = {
    "Buy": "var(--buy-color)",
    "Sell": "var(--sell-color)"
}

export const LIGHT_COLORS_SIDE: Record<Side, string> = {
    "Buy": "var(--buy-light-color)",
    "Sell": "var(--sell-light-color)"
}