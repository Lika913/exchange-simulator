import { Side } from "../types/order/side";

export const COLORS_SIDE_TEXT: Record<Side, string> = {
    "Buy": "var(--buy-text-color)",
    "Sell": "var(--sell-text-color)"
}

export const COLORS_SIDE_BUTTON: Record<Side, string> = {
    "Buy": "var(--buy-button-color)",
    "Sell": "var(--sell-button-color)"
}